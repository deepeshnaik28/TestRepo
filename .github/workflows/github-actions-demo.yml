name: GitHub Actions Demo
run-name: GitHub Actions Workflow new 22🚀
on: 
 pull_request:
   branches:
     - main
   types: [closed]
jobs:
  Explore-GitHub-Actions:
    runs-on: Windows-latest
    steps:
      - name: Hello world
        run: 
          | 
            echo "Hello w2orld from $GITHUB_BASE_REF"
            echo "Hello wor2ld from $GITHUB_HEAD_REF"
            echo "${{ github.event.pull_request.base.ref }}"
            echo "${{ github.event.pull_request.head.ref }}"
            $sourcebranch = ${{ github.event.pull_request.head.ref }}
            $WorkflowName =  $sourcebranch.Split("/")[0]
            $branchName =  $sourcebranch.Split("/")[1]
            echo "$WorkflowName"
            echo "$branchName"
            
        
