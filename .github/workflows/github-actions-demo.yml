name: GitHub Actions Demo
run-name: GitHub Actions Workflow new 23🚀
on: 
 push:
   branches:
     - release/2.0.2
jobs:
  Explore-GitHub-Actions:
    runs-on: Windows-latest
    steps:
      - name: Hello world
        run: 
          | 
            echo "Hello w2o3rld from $GITHUB_BASE_REF"
            echo "Hello wor2ld from $GITHUB_HEAD_REF"
            echo "${{ github.event.pull_request.base.ref }}"
            echo "${{ github.event.pull_request.head.ref }}"
            echo "OUTPUT_VALUE=123" >> $GITHUB_ENV
            
  Pass-Variable:
     needs: Explore-GitHub-Actions
     runs-on: Windows-latest
     steps:
      - name: Receive-Variable
        run: 
          | 
            echo "Hello w2o3rld from $GITHUB_BASE_REF"
            echo "Hello wor2ld from $GITHUB_HEAD_REF"
            echo "Output from Explore-GitHub-Actions = $OUTPUT_VALUE"
